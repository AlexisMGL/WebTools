[build]
  # Ensure submodules are fetched so plotly/jsparser/etc. are present in deploy
  command = "git submodule update --init --recursive || true"
  publish = "."

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
